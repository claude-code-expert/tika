# Tika 환경 변수 설정
# 이 파일을 .env.local로 복사한 후 값을 채워주세요.
# cp .env.example .env.local

# Vercel Postgres 연결 문자열
# Vercel 대시보드 → Storage → Postgres에서 확인
POSTGRES_URL=

# 로컬 개발 시 직접 PostgreSQL을 사용하는 경우:
# POSTGRES_URL=postgresql://username:password@localhost:5432/tika
