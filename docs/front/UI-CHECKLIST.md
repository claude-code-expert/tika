# UI 미구현 체크리스트

> **기준:** `public/demo/team/*.html` 프로토타입 대비 현재 앱 구현 상태
> **범례:** `[ ]` 미구현 · `[~]` 부분 구현 · `[x]` 구현 완료
> **최종 갱신:** 2026-03-07 (소스 직접 확인 기준)

---

## 1. 공통 레이아웃

### 사이드바 (`TeamSidebar.tsx`)

| # | 항목 | 데모 참조 | 상태 |
|---|------|-----------|------|
| 1 | MCP 설정 메뉴 항목 | `team.html` 사이드바 연동 섹션 | `[ ]` |
| 2 | 알림 내역 메뉴 항목 (미읽음 카운트 뱃지 포함) | `team.html` 사이드바 연동 섹션 | `[ ]` |

---

## 2. 대시보드 (`/team/:id`)

> 데모: `team.html`

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 3 | **진척률 도넛 차트** — 상태별(Done/In Progress/Todo/Backlog) 비율 | 차트 행 우측 | `[x]` `ProgressCard` 연결됨 |
| 4 | **Goal 카드 드릴다운** — Goal 클릭 시 하위 Story·Feature·Task 목록 슬라이드 확장 | Goal 진행률 섹션 | `[x]` `GoalProgressRow` 클릭 토글 + BFS 하위 항목 목록, 타입/상태 배지, 담당자 아바타 구현됨 |
| 5 | **MCP 서버 연동 패널** — Slack·GitHub 등 연동 서버 목록, 연결 상태 dot, 토글 on/off | 대시보드 하단 | `[ ]` |
| 6 | **알림 패널 (인라인)** — 최근 알림 5건, 채널 뱃지(slack/mcp/system), 읽음 여부 | 대시보드 하단 | `[ ]` |

---

## 3. 멤버 관리 (`/team/:id/members`)

> 데모: `team-members.html`

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 7 | **요약 통계 5개 카드** — 팀 멤버 수·총 할당 티켓·완료 티켓·총 Workday·팀 완료율 | 페이지 최상단 | `[x]` `members/page.tsx` summaryStats 구현됨 |
| 8 | **워크로드 테이블 — Workday 컬럼** — 멤버별 소요 근무일 수 | 워크로드 현황 테이블 | `[x]` `WorkloadHeatmap` non-compact 모드 마지막 컬럼에 `assigned * 2d` 표시 |
| 9 | **워크로드 테이블 — 부하 컬럼** — 적정/보통/과부하 텍스트 표시 | 워크로드 현황 테이블 | `[x]` `WorkloadHeatmap` non-compact 모드에 구현됨 |
| 10 | **멤버 상세 카드 — 이메일** — 카드 헤더에 이메일 노출 | 멤버 상세 카드 | `[x]` `MemberWorkload`에 email 추가, users LEFT JOIN, 카드 헤더 이름 아래 표시 |
| 11 | **멤버 상세 카드 — Workday stat** — 4번째 통계 항목으로 Workday 표시 | 멤버 상세 카드 stat 영역 | `[x]` `MemberDetailCard` 4번째 stat에 `assigned * 2 d` 표시됨 |

---

## 4. WBS / 간트 차트 (`/team/:id/wbs`)

> 데모: `team-wbs.html`

### 4-1. 페이지 상단 요약 카드

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 12 | **요약 카드 — Task 수** — Goal·Story·Feature·Task·전체 완료율 5개 카드 | 페이지 최상단 카드 행 | `[x]` `WbsClient` stats 5개 카드 구현됨 |

### 4-2. 툴바

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 13 | **Goal 선택 드롭다운** — 보고 싶은 Goal 선택 시 해당 Goal 하위만 간트에 표시 | 툴바 좌측 | `[x]` `WbsClient` selectedGoalId + 필터링 구현됨 |
| 14 | **Goal 정보 배너** — 선택 Goal의 기간·총 항목 수·완료 수·완료율 인라인 바 | 툴바 하단 | `[x]` `WbsClient` goalCounts 배너 구현됨 |
| 15 | **범례 (Legend)** — 툴바 우측에 완료/진행중/예정/미시작/지연 색상 범례 | 툴바 우측 | `[x]` `WbsClient` LEGEND 구현됨 |

### 4-3. 간트 그리드 (중앙 패널)

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 16 | **날짜 헤더 2단** — 상단 월 행 + 하단 일 행 | 간트 헤더 | `[x]` `GanttChart` monthGroups + weekdays 2단 헤더 구현됨 |
| 17 | **오늘 날짜 강조 + 자동 스크롤** — 오늘 열 색상 강조 및 초기 렌더 시 오늘로 스크롤 | 간트 날짜 열 | `[x]` `GanttChart` todayStr 강조 + useEffect 자동 스크롤 구현됨 |
| 18 | **지연(overdue) 바** — 마감일이 지난 미완료 항목을 빨간 바로 표시 | 간트 바 | `[x]` `getBarColor`에서 `endDate < todayStr` 시 빨간 바 처리 |
| 19 | **드래그 스크롤** — 중앙 패널 마우스 드래그로 가로 스크롤 | 간트 중앙 패널 | `[x]` `GanttChart` drag-to-scroll useEffect 구현됨 |
| 20 | **간트 바 tooltip** — hover 시 티켓명·기간·담당자 커스텀 팝업 | 간트 바 | `[x]` `fixed` 위치 커스텀 팝업, 마우스 추적, 담당자 아바타 표시 |

### 4-4. 패널 간 연동

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 21 | **3패널 레이아웃** — 좌측(작업 항목 트리)·중앙(날짜 그리드)·우측(담당자·우선순위·상태) | 간트 전체 | `[x]` `GanttChart` 3패널 + Goal 필터 연결 완료 |
| 22 | **행 hover 동기화** — 마우스 오버 시 3패널 동일 행 동시 하이라이트 | 간트 전체 | `[x]` `hoveredRow` state로 3패널 동기화 구현됨 |

---

## 5. 휴지통 (`/team/:id/trash`)

| # | 항목 | 상태 |
|---|------|------|
| 23 | **티켓 복구 기능** — 삭제된 티켓을 보드로 되돌리기 (soft delete → `deleted = false`) | `[x]` `PATCH /api/tickets/trash/:id` + 복구 버튼 구현 |
| 24 | **일괄 영구 삭제** — 전체 선택 후 한 번에 영구 삭제 | `[x]` 체크박스 + `DELETE /api/tickets/trash` 일괄 삭제 구현 |

---

## 남은 작업 요약 (미구현 4개 — MCP/알림 인프라 전제)

| 우선순위 | # | 항목 | 비고 |
|----------|---|------|------|
| 🟢 낮음 | 1 | 사이드바 MCP 설정 메뉴 | MCP 기능 구현 전제 필요 |
| 🟢 낮음 | 2 | 사이드바 알림 메뉴 (미읽음 뱃지) | 알림 기능 구현 전제 필요 |
| 🟢 낮음 | 5 | 대시보드 MCP 연동 패널 | MCP 기능 구현 전제 필요 |
| 🟢 낮음 | 6 | 대시보드 알림 패널 (인라인) | 알림 기능 구현 전제 필요 |
