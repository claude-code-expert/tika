# UI 미구현 체크리스트

> **기준:** `public/demo/team/*.html` 프로토타입 대비 현재 앱 구현 상태
> **범례:** `[ ]` 미구현 · `[~]` 컴포넌트 존재하나 미연결 · `[x]` 구현 완료

---

## 1. 공통 레이아웃

### 사이드바 (`TeamSidebar.tsx`)

| # | 항목 | 데모 참조 | 상태 |
|---|------|-----------|------|
| 1 | MCP 설정 메뉴 항목 | `team.html` 사이드바 연동 섹션 | `[ ]` |
| 2 | 알림 내역 메뉴 항목 (미읽음 카운트 뱃지 포함) | `team.html` 사이드바 연동 섹션 | `[ ]` |

---

## 2. 대시보드 (`/team/:id`)

> 데모: `team.html`

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 3 | **진척률 도넛 차트** — 상태별(Done/In Progress/Todo/Backlog) 비율 | 차트 행 우측 | `[~]` `ProgressDonut.tsx` 존재, 미연결 |
| 4 | **Goal 카드 드릴다운** — Goal 클릭 시 하위 Story·Feature·Task 목록 슬라이드 확장 | Goal 진행률 섹션 | `[ ]` |
| 5 | **MCP 서버 연동 패널** — Slack·GitHub 등 연동 서버 목록, 연결 상태 dot, 토글 on/off | 대시보드 하단 | `[ ]` |
| 6 | **알림 패널 (인라인)** — 최근 알림 5건, 채널 뱃지(slack/mcp/system), 읽음 여부 | 대시보드 하단 | `[ ]` |

---

## 3. 멤버 관리 (`/team/:id/members`)

> 데모: `team-members.html`

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 7 | **요약 통계 5개 카드** — 팀 멤버 수·총 할당 티켓·완료 티켓·총 Workday·팀 완료율 | 페이지 최상단 | `[ ]` |
| 8 | **워크로드 테이블 — Workday 컬럼** — 멤버별 소요 근무일 수 | 워크로드 현황 테이블 | `[ ]` |
| 9 | **워크로드 테이블 — 부하 컬럼** — 적정/보통/과부하 텍스트 표시 | 워크로드 현황 테이블 | `[ ]` |
| 10 | **멤버 상세 카드 — 이메일** — 카드 헤더에 이메일 노출 | 멤버 상세 카드 | `[ ]` |
| 11 | **멤버 상세 카드 — Workday stat** — 4번째 통계 항목으로 Workday 표시 | 멤버 상세 카드 stat 영역 | `[ ]` |

---

## 4. WBS / 간트 차트 (`/team/:id/wbs`)

> 데모: `team-wbs.html`

### 4-1. 페이지 상단 요약 카드

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 12 | **요약 카드 — Task 수** — 5번째 카드로 Task 수 표시 (현재 Goal·Story·Feature·완료율 4개만 있음) | 페이지 최상단 카드 행 | `[ ]` |

### 4-2. 툴바

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 13 | **Goal 선택 드롭다운** — 보고 싶은 Goal 선택 시 해당 Goal 하위만 간트에 표시 | 툴바 좌측 | `[ ]` |
| 14 | **Goal 정보 배너** — 선택 Goal의 기간·총 항목 수·완료 수·완료율 인라인 바 | 툴바 하단 | `[ ]` |
| 15 | **범례 (Legend)** — 툴바 우측에 완료/진행중/예정/미시작/지연 색상 범례 | 툴바 우측 | `[ ]` |

### 4-3. 간트 그리드 (중앙 패널)

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 16 | **날짜 헤더 2단** — 상단 월 행 + 하단 일 행으로 구성 (현재: 7일 간격 단순 라벨) | 간트 헤더 | `[ ]` |
| 17 | **오늘 날짜 강조 + 자동 스크롤** — 오늘 열 색상 강조 및 초기 렌더 시 오늘로 스크롤 | 간트 날짜 열 | `[ ]` |
| 18 | **지연(overdue) 바** — 마감일이 지난 미완료 항목을 빨간 바로 표시 | 간트 바 | `[ ]` |
| 19 | **드래그 스크롤** — 중앙 패널 마우스 드래그로 가로 스크롤 | 간트 중앙 패널 | `[ ]` |
| 20 | **간트 바 tooltip** — hover 시 티켓명·기간·담당자 커스텀 팝업 (현재 HTML title만 있음) | 간트 바 | `[ ]` |

### 4-4. 패널 간 연동

| # | 항목 | 위치(데모) | 상태 |
|---|------|-----------|------|
| 21 | **3패널 레이아웃** — 좌측(작업 항목 트리)·중앙(날짜 그리드)·우측(담당자·우선순위·상태) | 간트 전체 | `[~]` `GanttChart.tsx` 구현됨, Goal 필터 미연결 |
| 22 | **행 hover 동기화** — 마우스 오버 시 3패널 동일 행 동시 하이라이트 | 간트 전체 | `[ ]` |

---

## 5. 휴지통 (`/team/:id/trash`)

> 데모 없음 (자체 기획)

| # | 항목 | 상태 |
|---|------|------|
| 23 | **티켓 복구 기능** — 삭제된 티켓을 보드로 되돌리기 (soft delete → `deleted = false`) | `[ ]` |
| 24 | **일괄 영구 삭제** — 전체 선택 후 한 번에 영구 삭제 | `[ ]` |

---

## 우선순위 제안

| 우선순위 | 항목 번호 | 이유 |
|----------|-----------|------|
| 🔴 높음 | 3 | 대시보드 핵심 정보 — 컴포넌트 이미 존재, 연결만 하면 됨 |
| 🔴 높음 | 1, 2 | 사이드바 내비게이션 불완전 — MCP·알림 진입 경로 없음 |
| 🔴 높음 | 13, 14 | WBS Goal 필터·배너 — 대형 프로젝트에서 간트가 너무 길어짐 |
| 🟡 중간 | 16, 17, 18 | WBS 간트 그리드 품질 — 날짜 헤더·오늘 강조·지연 바 |
| 🟡 중간 | 7, 8, 9 | 멤버 페이지 통계 — 데모와 시각적 차이 가장 큰 부분 |
| 🟡 중간 | 19, 22 | WBS 인터랙션 — 드래그 스크롤·행 hover 동기화 |
| 🟢 낮음 | 5, 6 | MCP/알림 대시보드 패널 — 별도 설정 페이지로 대체 가능 |
| 🟢 낮음 | 23, 24 | 휴지통 UX 개선 |
